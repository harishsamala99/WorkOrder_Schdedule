prompt-1: 
Adding the 3 dots on the hover.

prompt-2:
How can I display the dropdown so that it:In a work order timeline UI, each work order bar has a three-dot actions button.

When the dropdown menu opens, it overlaps the next row and can be clipped
because the row has a fixed height and overflow constraints.

How can I display the dropdown so that it:
- is fully visible
- is not clipped by the row container
- appears above all rows
- and works correctly inside a horizontally scrollable container in Angular?


prompt-3:
I needed help designing how to calculate bar positions for a timeline
with day / week / month zoom levels.

In my work order scheduling timeline, I currently support Day, Week and Month views.

How can I add an additional "Hour" view so that:
- the timeline header shows hours
- work order bars are positioned based on start and end time (not only date)
- the existing day / week / month logic is reused as much as possible
- and the timeline still works correctly with horizontal scrolling in Angular?